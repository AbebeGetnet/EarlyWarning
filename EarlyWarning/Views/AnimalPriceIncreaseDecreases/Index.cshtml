@model IEnumerable<EarlyWarning.Models.AnimalPrice.AnimalPriceIncreaseDecrease>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ReasonName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReasonType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ReasonName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReasonType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AnimalPriceIncreaseDecreaseId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AnimalPriceIncreaseDecreaseId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AnimalPriceIncreaseDecreaseId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
